# dotfiles — main — 2026-07-29

## Goal

Track the global Codex policy without versioning private or mutable Codex state.

## Completed

- Added `codex-config/.codex/AGENTS.md` as the tracked source of truth.
- Added `codex-config` to the stateful Stow packages.
- Documented the package and its `--no-folding` requirement.
- Replaced `~/.codex/AGENTS.md` with a symlink to the tracked file.

## Validation

- The live symlink resolves to `codex-config/.codex/AGENTS.md`.
- The pre-migration backup, tracked source, and live file have identical content.
- `stow --no-folding -nv codex-config`, `make stow-preview`, and
  `git diff --check` pass.

## Next

- Review and approve the proposed commit:
  `chore(codex): track global AGENTS policy with Stow`
