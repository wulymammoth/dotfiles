# Local Codex cleanup checkpoint — 2026-09-10

Executed the approved GSD retirement handoff from the clean startup checkout on
`main` at `7d5464fc6656162d0119b4174befb40e77b0ff9e`. The handoff's five prior file
hashes matched that commit. The cleanup was local and uncommitted at verification;
the owner subsequently authorized one cleanup commit and pushing `main` to `origin`.

GSD agent and hook discovery is retired; 73 Codex-owned paths are privately
archived. Shared GSD skill files retain all 142 disablements. Astra/medium and
profile inheritance are preserved. The memory repair tool is unchanged and its
tests are decoupled from ordinary profile/Stow checks. The launch/resume skill
name now matches the installed skill.

See `docs/skill-maintenance/2026-09-10/README.md` for exact scope, archive/restore
locations, successful tests, fresh native discovery, and runtime limits. No
worker/model canary, provider action, or memory-data mutation was performed.
Commit/push authorization covers only the cleanup files; unrelated Docker Desktop
edits in the Bash and Zsh profiles remain outside that commit. A new conversation
is needed to refresh its advertised catalog.
