# Maestro and agent-policy integration checkpoint

## Authority and scope

The user approved conditional push, integration, and activation, then supplied
the Maestro session's explicit primary-checkout handoff to audit session
`01a071a2-c1aa-7872-8399-873f82a32e07`. The audit independently verified the
clean physical root `/Users/wulymammoth/dotfiles` before accepting ownership.

Integration preserves this order and both reviewed histories:

1. Main base `7904ec58b3584396b0d5af3952d66952b84dcf2f` fast-forwards to
   Maestro `bef0a3ab2d75b713d7615c28ff5dd64dcdb80a0f`.
2. Policy head `490693b3360f72c7db78022a1c7489197a2183cd`, including its prior
   milestone `098026bd8c2f2f25cdb16175a701aac5ecc9dc84`, merges without conflicts.

The unused local main ref was advanced with an expected-old comparison before
switching to its identical Maestro tree. This avoided temporarily removing
active Stow-linked sources. Maestro routing in global `AGENTS.md` is retained.

## Verification at this local checkpoint

- Native policy helper: **23 tests passed**.
- Installed Engram MCP contract in isolated temporary storage: **1 test passed**.
- Maestro: **21 tests**, no-folding/PATH integration, and ShellCheck passed.
- Profile, Codex Stow, worktree-session, and ctx integration checks passed.
- Maestro Ruff and staged diff checks passed.

The initial Maestro wrapper test returned exit 126 because its temporary `HOME`
made the ambient `~/.asdf/shims/python3` unusable. A minimal interpreter-only
reproduction confirmed the same failure; an explicit
`PATH=/opt/homebrew/bin:/usr/bin:/bin` resolved it. The unchanged Maestro suite
then passed with `PYTHON=/opt/homebrew/bin/python3`. No global PATH or source fix
was made. Run temporary-home checks with an explicitly usable interpreter path.

Raw logs are under `/tmp/agents-audit-2026-09-05/combined-integration/` and are
ephemeral evidence, not repository authority.

## Continuation and remaining evidence

This note records the verified **pre-publication** checkpoint. Independent
integration review, the integration commit, remote publication, and native
activation follow it; resolve their actual state from live Git and the hosted
pull request rather than inferring completion from this note.

The integrated tracked policy retains Engram MCP for ADR/decision memory and
ctx for focused provenance. The Engram shell plugin remains disabled, both
instruction-file overrides remain absent, and no memory store is removed or
merged. Native home activation must use the version-guarded interface in
`docs/codex-memory-policy.md` and a fresh readback after applying.

The profile test's provider-backed hook/policy/Superpowers `PENDING_CANARY`
remains separate from offline configuration proof. Existing sessions retain
startup instructions. Maestro native-driver/app regression tests remain unrun;
previous Java 26 startup warnings are not a successful app-test claim. No
simulator, device, provider configuration, store, release, or cleanup action is
included in this integration.
